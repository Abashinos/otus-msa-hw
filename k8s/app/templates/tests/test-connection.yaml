apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otus-hw-app.fullname" . }}-test-connection"
  labels:
    {{- include "otus-hw-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otus-hw-app.fullname" . }}:{{ .Values.serverService.port }}']
  restartPolicy: Never
